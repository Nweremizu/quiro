{
	"recommendations": [
		// Inline icon previews + searchable autocomplete for `~icons/<collection>/<name>`
		// imports (unplugin-icons) — type `~icons/` or a bare `<IconXxx>` and it
		// shows a live icon picker instead of needing to look names up on
		// icon-sets.iconify.design.
		"antfu.iconify",
		// Matches this repo's actual formatter/linter (biome.json).
		"biomejs.biome"
	]
}
